<header
    class="flex items-center justify-between md:hidden bg-[var(--surface-color)] p-6 shadow-lg border-b border-[var(--border-color)]">
    <h1 class="text-lg font-black">CryptUP</h1>
    <button onclick="toggleSidebar()"
        class="text-[var(--text-primary)] text-2xl focus:outline-none hover:text-[var(--primary-color)] transition-colors"
        aria-label="Toggle sidebar">
        <span id="hamburger">☰</span>
    </button>
</header>